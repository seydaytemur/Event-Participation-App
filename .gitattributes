# Exclude library scripts and styles from language statistics
wwwroot/lib/** linguist-vendored
*.min.js linguist-vendored
*.min.css linguist-vendored

# Optional: Treat .cshtml as standard HTML if C# stats are still low, 
# or force it to be counted as C# (though it is mixed).
# *.cshtml linguist-language=C#
